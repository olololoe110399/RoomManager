<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qunlphngtr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.LoginActivity"></activity>
        <activity android:name=".Activities.AddBillActivity" />
        <activity android:name=".Activities.BillDetailActiviy" />
        <activity android:name=".Activities.AddContractActivity" />
        <activity android:name=".Activities.BillActivity" />
        <activity android:name=".Activities.ContractDetailActivity" />
        <activity android:name=".Activities.ContractActivity" />
        <activity android:name=".Activities.RoomActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>